const asyncHandler = require("express-async-handler");

// This is just a re-export of express-async-handler
// We can add custom error handling here if needed in the future
module.exports = asyncHandler;
